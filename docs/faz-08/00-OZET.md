# FAZ 08 — Roller

## Kapsam

Spatie Role (tenant teams) CRUD + permission sync.

## Bileşenler

- `RoleService`
- `RoleController`, `RolePolicy`
- `StoreRoleRequest` / `UpdateRoleRequest`
- Kullanıcısı olan rol silinemez
